<h3>Questions</h3>


<table border="1">
  <% @questions.each do | question | %>
  <tr>
    <td><%= question.title %></td>
    <td><%= truncate(question.body, length: 20, separator: ' ') %></td> 
  </tr>
  <% end %>

</table>